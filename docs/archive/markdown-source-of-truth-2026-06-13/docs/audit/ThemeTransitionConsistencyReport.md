# Theme Transition Consistency Report

Theme switching updates `data-eden-theme`, `data-visual-theme`, `data-appearance` and CSS variables at runtime. Light/dark appearance is independent from visual theme. Legacy stored theme ids normalize to the new six-theme model to avoid preference breakage.
