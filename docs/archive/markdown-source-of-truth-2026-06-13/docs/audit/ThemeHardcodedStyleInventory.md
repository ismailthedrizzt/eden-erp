# Theme Hardcoded Style Inventory

The global bridge maps common hardcoded Tailwind blue/gray/slate/zinc/status utilities to Eden tokens. Residual hardcoded classes still exist in individual feature screens, but core shared surfaces are overridden by token variables. Remaining cleanup should be component-by-component, prioritizing high-traffic forms and module-specific dashboards.
