# Theme Token Architecture

<!-- source-of-truth-standard: contract overrides markdown -->

Each theme provides light and dark sets for base, accent, status, shell, page banner, card, form, table/smart list, badge, wizard, interaction, shape, shadow, density, icon and decorative motif tokens. Production must not fall back to default blue for missing core surfaces.
