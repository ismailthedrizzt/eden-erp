# Technical Debt and Migration Plan

Bu dokuman Eden ERP platform mimarisine geciste bilerek korunan wrapper, fallback ve kademeli refactor borclarini gorunur tutar. Amac eski davranisi bozmadan Domain Service, Process, Transaction Boundary, Outbox, Audit, Readiness, Visibility ve Action Center katmanlarina guvenli gecisi surdurmektir.

Her kalem su alanlarla izlenir: `area`, `file/module`, `current_state`, `target_state`, `priority`, `suggested_followup_prompt`.

## P1

| area | file/module | current_state | target_state | priority | suggested_followup_prompt |
| --- | --- | --- | --- | --- | --- |
| FastAPI migration bridge | `app/api/**`, `backend/app/api/v1/**` | Next API route'lari bugun hem BFF hem TS backend logic tasiyor. | Next API route'lari proxy/adaptor olur; core business logic FastAPI endpointlerine tasinir. | P1 | "P0 operation route'lari icin FastAPI endpointlerini ekle ve Next route'lari proxy/adaptor'a indir." |
| Next proxy fallback removal | `app/api/**`, `lib/backend/fastApiProxy.ts` | FastAPI-backed route gruplari `proxy_to_fastapi_with_legacy_fallback` statulendi; `FASTAPI_BASE_URL` yoksa TS fallback bazi rotalarda calisir. | Staging FastAPI dogrulamasi tamamlandikca fallbackler silinsin ve route'lar `proxy_to_fastapi` seviyesine insin. | P1 | "Next API proxy envanterindeki legacy fallback route'larini staging FastAPI ile dogrula ve fallback kodlarini kaldir." |
| Branch TS fallback removal | `app/api/companies/[company_id]/official-changes/branch-opening/**`, `app/api/companies/[company_id]/official-changes/branch-closing/**` | FastAPI proxy eklendi; `FASTAPI_BASE_URL` yoksa TS fallback calisiyor. | Staging DB dogrulamasi sonrasi TS fallback silinsin ve route sadece BFF/proxy kalsin. | P1 | "Branch opening/closing FastAPI endpointlerini staging DB ile dogrula, Next route'larindan legacy TS fallback'i kaldir." |
| Company official changes TS fallback removal | `app/api/companies/[company_id]/official-changes/{title,address,public-registration,nace,activity-subject}-change/**` | FastAPI proxy eklendi; `FASTAPI_BASE_URL` yoksa TS fallback calisiyor. | Staging DB dogrulamasi sonrasi TS fallback silinsin, `_shared.ts` official change business logic kaynagi olmaktan ciksin. | P1 | "Company official changes FastAPI endpointlerini staging DB ile dogrula, Next route'larindan legacy TS fallback'i kaldir ve `_shared.ts` split planini uygula." |
| Card CRUD TS fallback removal | `app/api/companies/**`, `app/api/companies/partners/**`, `app/api/companies/representatives/**` | Company/partner/representative card CRUD FastAPI endpointleri eklendi; Next route'lar `proxy_to_fastapi_with_legacy_fallback` olarak kalir. | Staging dogrulamasi sonrasi TS card CRUD fallbackleri silinsin; Next route sadece BFF/proxy olsun. | P1 | "Company, Partner ve Representative card CRUD FastAPI endpointlerini staging DB ile dogrula ve Next route'larindan TS CRUD fallbacklerini kaldir." |
| Master identity sync Python hardening | `backend/app/domains/{company,partners,representatives}`, `lib/identity/*` | Card CRUD Python'a tasindi, fakat person/organization master sync davranisinin bir kismi legacy fallback tarafinda zengin. | Python card servisleri master identity/contact sync ve duplicate rules icin canonical hale gelsin. | P1 | "FastAPI card CRUD servislerine master identity/contact sync ve duplicate identity hardening ekle." |
| Document upload/storage Python adapter | `backend/app/domains/{company,partners,representatives}`, `app/api/uploads/**` | Card document metadata patch edilebilir, binary upload/signed URL adapter halen Next UI adapter. | Document domain/storage adapter Python tarafinda standardize edilsin. | P2 | "Company/Partner/Representative document metadata ve storage adapterini FastAPI document domain servisine tasi." |
| Python auth hardening follow-up | `backend/app/core/security.py`, `backend/app/policies/access_context.py`, `lib/backend/fastApiProxy.ts` | Supabase JWT HS256 verification, tenant membership validation, permission loading, company scope loading and internal token guard landed. | JWKS crypto runtime dependency, auth denial audit logging, role/membership schema finalization and e2e Supabase session tests complete. | P1 | "FastAPI auth katmanina JWKS runtime dependency, auth denial audit ve Supabase session e2e testlerini ekle." |
| Observability production integration | `backend/app/core/{logging,metrics,error_tracking}.py`, workers, deployment | Structured logging, request/correlation IDs, in-memory metrics, slow query hooks and no-op error tracking extension landed. | OpenTelemetry tracing, Sentry, Prometheus/Grafana and centralized structured log storage provisioned. | P1/P2 | "FastAPI observability katmanini OpenTelemetry, Sentry ve Prometheus/Grafana deployment ile production'a bagla." |
| Performance index rollout | `supabase/migrations/20260528_performance_indexes.sql`, staging DB | Safe non-unique performance indexes prepared for critical list/platform tables. | Staging EXPLAIN results reviewed, large-table production rollout uses CONCURRENTLY where required. | P1 | "Staging DB'de performance index migration'ini uygula, EXPLAIN sonuclarini kaydet ve production rollout planini netlestir." |
| Load test staging integration | `scripts/load-test.js`, package scripts | Platform load-test scenarios and local/staging guard are ready. | CI/staging scheduled smoke load test runs with tenant/session fixture and reports p95/p97.5 regressions. | P2 | "Eden ERP load-test senaryolarini staging pipeline'a bagla ve latency budget regression raporu uret." |
| Audit/outbox/process retention | `docs/architecture/DataRetentionAndVolumePlan.md`, workers | Volume and retention policy documented; archive jobs not implemented. | Completed outbox archive, audit partition/archive and process cold-storage jobs implemented. | P2 | "Outbox completed archive ve audit/process retention worker planini uygulamaya al." |
| Production deploy hooks | `.github/workflows/ci.yml`, deployment platform | CI validates build/test/OpenAPI/Docker; provider-specific deploy hooks are not wired. | Staging and production deployment hooks run after approvals with smoke checks. | P1 | "GitHub Actions workflow'una staging deploy, smoke test ve manual production approval job'larini ekle." |
| Secret manager integration | env docs, deployment platform | Env examples and env safety script exist; real secret manager wiring is deployment-provider specific. | Vercel/VPS/Kubernetes secrets managed centrally and rotated. | P1 | "Eden ERP production secret manager entegrasyonunu secilen deploy platformuna gore uygula." |
| TS backend removal controls | `scripts/check-backend-migration-status.js`, `scripts/check-import-boundaries.js`, `docs/architecture/RemainingTsBackendInventory.md` | Boundary and inventory scripts now report proxy-only violations, temporary fallbacks and remaining TS backend surfaces. | Temporary fallback warnings trend to zero; `boundaries:check --strict` can become CI blocking once staging verification removes fallbacks. | P1 | "RemainingTsBackendInventory P1 satirlarini kullanarak migrated route fallbacklerini kaldir ve boundaries:check strict mode'a hazirla." |
| Import boundary warnings | `components/action-center/*`, `components/ai/*`, company pages | Some frontend files import TS backend-core namespaces for shared shapes/registries. | Replace these imports with generated/shared contract-only modules so frontend does not depend on backend runtime folders. | P1 | "boundaries:check warning listesindeki frontend importlarini generated/shared contract importlarina tasi." |
| Generated OpenAPI client full adoption gaps | `lib/services/companyService.ts`, `lib/generated/backend-client/*` | Generated types exist, but frontend wrappers still keep manual DTO bridges for compatibility. | Service methods use generated endpoint request/response contracts and manual DTOs are marked deprecated or removed. | P1 | "companyService endpoint ailelerini generated OpenAPI paths/schema tipleriyle tam tipleyip manual DTO tekrarlarini azalt." |
| Branch DB integration tests | `backend/app/domains/branches/*`, `backend/app/tests/*` | Schema validation testleri hazir; DB transaction testleri test database gerektiriyor. | Branch opening/closing icin isolated test DB veya transactional fixture ile integration test yazilsin. | P1 | "FastAPI branch opening/closing icin test database fixture ve integration testleri ekle." |
| Company official changes DB integration tests | `backend/app/domains/company/*`, `backend/app/tests/*` | Schema/NACE/lifecycle validation testleri hazir; public table sync ve transaction insert DB fixture gerektiriyor. | Title/address/public/NACE/activity subject icin transaction, public sync, outbox/audit best-effort entegrasyon testleri yazilsin. | P1 | "FastAPI company official changes icin isolated test DB fixture ve transaction/public table integration testleri ekle." |
| Capital/Ownership DB integration tests | `backend/app/domains/company/capital.py`, `backend/app/domains/ownership/*` | Capital increase endpointleri ve ownership validation unit testleri hazir; company capital update, ownership transaction insert ve idempotency DB fixture gerektiriyor. | Seeded test DB ile capital transaction, ownership transaction, outbox/audit ve duplicate client_request_id entegrasyon testleri yazilsin. | P1 | "FastAPI capital increase icin isolated test DB fixture ve ownership transaction integration testleri ekle." |
| Representative authority TS fallback removal | `app/api/companies/representatives/[id]/route.ts`, `backend/app/domains/representatives/*` | Authority transaction endpointi FastAPI'de; Next route `authority_action` icin proxy, card update icin TS fallback kullaniyor. | Staging DB dogrulamasi sonrasi authority transaction fallback silinsin; representative card create/update/list endpointleri Python proxy'ye alinsin. | P1 | "FastAPI representative authority endpointini staging DB ile dogrula, Next representative route'undan authority transaction TS fallback'ini kaldir ve card endpointlerini proxy yap." |
| Representative current authority integration tests | `backend/app/domains/representatives/*`, `v_current_representative_authorities` | Scope validation ve transaction validation unit testleri var; DB view hydration ve duplicate/idempotency DB fixture gerektiriyor. | Branch/organization/facility scoped authority transaction ve current authority view integration testleri yazilsin. | P1 | "Representative Authority FastAPI icin seeded test DB fixture ile scope transaction/current authority integration testleri ekle." |
| Ownership transaction TS fallback removal | `app/api/ownership-transactions/**`, `backend/app/domains/ownership/*` | Transaction creation FastAPI'ye proxyleniyor; `[id]/**` approve/reject/cancel/reverse route'lari TS fallback olarak duruyor. | Ownership workflow route'lari FastAPI endpointlerine tasinsin ve Next sadece BFF/proxy kalsin. | P1 | "Ownership transaction approve/reject/cancel/reverse route'larini FastAPI'ye tasi ve Next fallback'leri kaldir." |
| Ownership transaction DB integration tests | `backend/app/domains/ownership/*`, `v_current_ownership` | Current ownership ve transaction validation unit testleri var; DB view, idempotency, partner status update, outbox/audit icin seeded DB testi yok. | Initial partnership, share transfer, ownership exit ve reversal icin transaction + current ownership integration testleri yazilsin. | P1 | "Ownership Transactions FastAPI icin seeded test DB fixture ile current ownership ve idempotency integration testleri ekle." |
| Process/Outbox/Audit TS fallback removal | `app/api/processes/**`, `app/api/tasks/**`, `app/api/approvals/**`, `app/api/audit/**`, `app/api/action-center/**`, `app/api/cron/outbox-dispatch/route.ts` | FastAPI proxy eklendi; `FASTAPI_BASE_URL` yoksa TS fallback calisiyor. | Python process/audit/outbox/action-center endpointleri staging DB ile dogrulandiktan sonra TS backend fallback silinsin. | P1 | "Process, Audit, Action Center ve Outbox cron route'larini staging FastAPI ile dogrula; Next route'larindan TS fallback'i kaldir." |
| Python worker deployment | `backend/app/workers/outbox_worker.py`, deployment config | Outbox worker CLI MVP hazir; production scheduler/supervisor henuz yok. | Worker deployment, stale-lock release, retry alarm ve metrics/logging standardi tanimlansin. | P1 | "Python outbox worker icin deployment manifest, metrics/logging ve retry alerting ekle." |
| Python audit/process permission hardening | `backend/app/api/v1/{audit,processes,tasks,approvals}.py`, `backend/app/core/security.py` | Header tabanli migration context kullaniliyor; permission/scope enforcement MVP seviyesinde. | Supabase JWT, permission registry ve tenant/company scope guard Python endpointlerinde zorunlu olsun. | P1 | "FastAPI process/audit/action-center endpointlerine JWT permission ve company/branch scope guard ekle." |
| Python policy/readiness auth hardening | `backend/app/policies/*`, `backend/app/setup/*`, `backend/app/api/v1/{policy,setup,integrity}.py` | Permission ve readiness karar katmanlari Python'da canonical oldu; migration bridge header context kullaniliyor. | Supabase JWT claim validation, DB-backed role permission load ve tenant module settings zorunlu hale gelsin. | P1 | "FastAPI policy/readiness/integrity endpointlerine Supabase JWT permission loading ve DB-backed module settings ekle." |
| Full Action Guide Python resolver | `app/api/ai/action-guide/route.ts`, `backend/app/policies/action_eligibility.py` | Canonical eligibility Python'da; intent matching halen TS deterministic resolver'da. | Intent resolver FastAPI'ye tasinsin ve frontend sadece generated client/BFF proxy tucketsin. | P2 | "Action Guide intent resolver'i FastAPI'ye tasi ve TS route'u eligibility proxy/adaptor seviyesine indir." |
| Continuous integrity scan bridge | `backend/app/integrity/*`, `backend/app/api/v1/action_center.py` | Operation-time integrity guard MVP var; surekli tarama ve Action Center item bridge yok. | Scheduled integrity scan, persistent warning/blocking items ve action center source adapter eklensin. | P2 | "Python integrity checker icin scheduled scan ve Action Center item bridge ekle." |
| Obsolete compatibility cleanup | `LegacyObsoleteCodeAudit.md` listesi | Bazi wrapper/alias/fallback kodlari eski davranisi koruyor gibi duruyor. | Sadece canli migration bridge olanlar planli kalir; digerleri silinir veya canonical helper'a baglanir. | P1 | "LegacyObsoleteCodeAudit P0 kalemlerini tek tek sil/merge/replace et ve import kirilmalarini typecheck ile dogrula." |
| Build/typecheck hygiene | Platform geneli | Final kontrolde typecheck/build temiz tutulmali; yeni fazlarda PWA generated file churn ve import sinirlari tekrar kirilabilir. | Her mimari faz sonunda typecheck/build/platform consistency otomatik calissin. | P1 | "Eden ERP icin platform:check script'i ekle, typecheck/build/platform consistency ve git diff --check'i tek komutta kos." |
| Field control backend enforcement | `app/api/companies/*`, `components/ui/EntityForm.tsx`, `lib/field-controls/*` | Locked field helper UI'da gorunuyor; tum PATCH route'larinin ayni backend guard'i kullandigi adim adim dogrulanmali. | Operation-controlled alanlar backend'de tek canonical guard ile reddedilmeli. | P1 | "Tum company/partner/representative/branch PATCH route'larini Field Control Guard ile standardize et." |
| Critical transaction atomicity | `lib/operations/orchestrators/*`, `lib/operations/transaction-boundary/*` | Branch opening/closing transaction boundary kullanmaya basladi; capital/ownership/representative operasyonlari kademeli. | Kritik mutation zincirleri Transaction Boundary + RPC fallback contract ile atomic hale gelsin. | P1 | "Sermaye Artirimi ve Representative Authority icin Transaction Boundary fallback/RPC contract entegrasyonunu tamamla." |
| Scope/security coverage | `app/api/companies/*`, `app/api/audit/*`, `app/api/action-center/*` | Tenant/company/branch scope helperlari yayginlasti; eski route'larda tekrar eden kontrol var. | Route seviyesinde scope guard ve response standardi tek helper uzerinden kullanilsin. | P1 | "Company, branch, partner, representative API route'larinda scope guard standardizasyonu yap ve regression test checklist'i ekle." |
| Module readiness crash prevention | `lib/setup/*`, `lib/modules/moduleGuards.ts`, module API routes | Missing infrastructure durumlari readiness diline cevrildi; tum yeni moduller ayni contract ile eklenmeli. | Module guard her route/action oncesinde disabled/unlicensed/setup_required/dependency_missing durumunu is diliyle donsun. | P1 | "Module guard'i company/branch/representative/process/audit route'larina ortak adaptor olarak uygula." |

## P2

| area | file/module | current_state | target_state | priority | suggested_followup_prompt |
| --- | --- | --- | --- | --- | --- |
| Domain service migration | `app/api/companies/[company_id]/official-changes/_shared.ts`, `lib/domains/*`, `backend/app/domains/*` | TS domain servisleri Python migration icin sozlesme/prototip rolunde. | Domain-specific query/mutation kodlari FastAPI/Python domain service'e tasinsin. | P2 | "Official changes _shared helperlarini FastAPI Company/Branch/Organization/Facility servislerine tasi ve Next route'u proxy yap." |
| OpenAPI generated client | `lib/generated/backend-client/*`, `lib/backend/backendClient.ts`, frontend API wrappers | OpenAPI refresh scriptleri, generated `types.ts`, shared adapter ve ilk `companyService` endpoint type adoption'i hazir. | Manual DTO tekrarlarini azaltmak icin service wrapper methodlari kademeli generated endpoint response/request type'lariyla tiplenir ve uygun yerde generated client'a delege eder. | P1 | "companyService ve ilgili servis wrapperlarini generated OpenAPI endpoint type'lariyla genislet; manual DTO bridge type'larini deprecated olarak isaretle." |
| Projection DB view/index optimization | `backend/app/projections/*`, Supabase views | Python projection endpointleri MVP olarak tablo/view fallback ile calisir. | High traffic company/branch/partner/representative listeleri indexed DB view ve query plan kontrolleriyle optimize edilsin. | P1 | "FastAPI projection endpointleri icin DB view/index optimizasyonu ve slow query smoke testleri ekle." |
| Frontend generated client adoption | `lib/generated/backend-client/*`, `lib/services/companyService.ts` | OpenAPI generation script ve adapter hazir; wrapperlar henuz yaygin sekilde generated types kullanmiyor. | Company/branch/capital/ownership/representative service methodlari generated OpenAPI types ile tiplenmeye baslasin. | P1 | "companyService FastAPI proxy methodlarini generated OpenAPI types ile tipleyerek manual DTO tekrarlarini azalt." |
| Representative authority projection completeness | `backend/app/projections/representative.py`, `v_current_representative_authorities` | Current authority hydrate view varsa calisir; view scope field eksiginde warning doner. | View branch/organization/facility scope label alanlarini eksiksiz dondursun. | P1 | "v_current_representative_authorities view'unu scope field completeness icin guncelle ve projection integration testi ekle." |
| RPC / transaction migration | `supabase/migrations/*operation_rpc*`, `lib/operations/transaction-boundary/*`, `backend/app/services/*` | RPC contract ve TS fallback hazir; Python transaction unit-of-work yeni basladi. | Branch, capital, ownership ve representative mutationlari Python transaction boundary veya DB RPC ile atomic calissin. | P2 | "FastAPI Transaction Boundary unit-of-work katmanini kur ve Branch Opening/Closing icin ilk Python uygulamasini yap." |
| Projection optimization | `lib/read-models/projections/*`, Supabase views | Bazi projection'lar view yoksa query fallback ile calisiyor. | Yuksek trafikli listeler indexed DB view/projection ile optimize edilsin. | P2 | "Company, branch, ownership ve representative list projection fallback'lerini view/index optimizasyonuna tasiyarak performans olc." |
| Process UI integration | `app/api/processes/*`, `app/api/tasks/*`, UI process pages | Process/task/approval endpointleri ve Action Center kaynaklari hazir; tam workflow UI sinirli. | Surec detay, task tamamlama ve approval karar ekranlari kullanici scope'una gore tamamlanmis olsun. | P2 | "Process Center UI'ini task/approval/detail akislariyla genislet ve Action Center action buttonlarini bu ekranlara bagla." |
| Outbox real handlers | `backend/app/domains/outbox/handlers.py` | Handlerlar idempotent no-op MVP olarak calisiyor. | Projection invalidation, notification, audit ve AI context refresh handlerlari gercek yan etkilere baglansin. | P2 | "Python outbox handlerlarini projection invalidation ve Action Center notification etkileriyle gercek implementasyona tasi." |
| Audit coverage | `lib/audit/*`, API route groups | Merkezi audit service hazir; tum belge, permission deny ve official change route'lari ayni coverage'a sahip degil. | Kritik API attempt, permission/policy deny, document upload/delete ve operation complete/fail kapsami standart olsun. | P2 | "Audit coverage matrix olustur ve eksik route gruplarina recordAudit wrapper'i ekle." |
| Action Guide intelligence | `lib/action-guide/*`, `components/ai/*` | Deterministic registry tabanli matcher ve eligibility MVP calisiyor. | Dusuk confidence durumunda LLM sadece registry actionlari arasindan secim yapsin. | P2 | "Action Guide icin registry-constrained LLM intent refinement ekle ve schema validation ile halusinasyonu engelle." |
| Integrity to Action Center bridge | `lib/integrity/*`, `lib/action-center/*` | Integrity precheck sonuclari action guide/orchestrator tarafindan kullanilabilir; surekli tarama yok. | Blocking/warning integrity sonuclari ilgili kayit ve kullanici scope'unda Action Center item'i olabilsin. | P2 | "Operation precheck integrity warnings icin Action Center adapter'i ekle ve branch closing representative impact'i item'a cevir." |

## P3

| area | file/module | current_state | target_state | priority | suggested_followup_prompt |
| --- | --- | --- | --- | --- | --- |
| UI polish | `components/onboarding/*`, `components/action-center/*`, `components/modules/*` | Guided Tour, Action Center ve ModuleUnavailable MVP olarak hazir. | Mobil, keyboard focus, reduced motion ve empty state polish tamamlanmali. | P3 | "Guided Tour ve Action Center icin responsive/focus/reduced-motion UI polish turu uygula." |
| Admin audit UI | `app/app/sistem/audit/page.tsx`, `components/audit/*` | Audit sayfasi placeholder/MVP seviyesinde. | Filtrelenebilir timeline, old/new masked value detail ve export yetkileri eklensin. | P3 | "Audit admin UI'ini filtreli timeline ve masked diff detaylariyla genislet." |
| Advanced process designer | `lib/process/*`, process docs | Process registry kod tabanli tanimlarla calisiyor. | Yetkili kullanicilar icin process designer veya admin editor hazirlansin. | P3 | "Process definition editor MVP'si tasarla; registry export/import ve validation ekle." |
| Module marketplace polish | `app/app/sistem/module-licenses/page.tsx`, `app/app/sistem/kurulum/page.tsx` | License/readiness ve setup ekranlari ayrik MVP'ler olarak calisiyor. | Modul lisansi, kurulum adimi ve feature flag gorunumleri tek rehberli kurulum deneyimine baglansin. | P3 | "Module license, readiness ve setup action ekranlarini tek moduler kurulum deneyimi olarak birlestir." |
| Platform consistency automation | `lib/platform/platformConsistencyCheck.ts` | Dev helper critical/warning raporu uretiyor; package script'e bagli degil. | CI/release oncesi critical inconsistency otomatik fail etsin. | P3 | "npm run platform:check script'i ekle ve platform consistency helper'ini CI uyumlu JSON/text output ile calistir." |
