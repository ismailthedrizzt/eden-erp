# Alembic Migrations

<!-- source-of-truth-standard: contract overrides markdown -->

Alembic migrations for the FastAPI core backend will live here. Supabase/PostgreSQL remains the database platform; Python backend migrations should be coordinated with existing Supabase SQL migrations until the migration ownership model is finalized.
